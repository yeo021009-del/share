# 파크핏 — 작업 규칙

## 테스트 작성 규칙

- 소스 문자열 존재/부재를 검사할 때는 반드시 **주석과 문자열 리터럴을 제거한 뒤의
  코드**에 대고 검사한다. 이 목적의 공용 헬퍼는 [`tests/srccheck.py`](tests/srccheck.py)
  하나뿐이고, 전부 그것을 쓴다.

  | 무엇을 보나 | 쓸 것 |
  |---|---|
  | 코드가 이 동작을 하는가 (함수 호출·클래스 부착·상수 참조) | `srccheck.code()` |
  | 이 문구가 화면에 나가는가 (문자열 리터럴 자체가 검사 대상) | `srccheck.no_comments()` |

  헷갈리면 `code()` 다. 화면 문구를 볼 때만 예외다.

- **사유**: 설명 주석에 검사 대상 문자열이 들어가 테스트가 통과한 사고가 **5회**
  발생했다. 방향에 따라 증상이 다르다.

  - **있나** 를 볼 때 — 주석이 대신 걸려 **조용히 통과**한다. 이게 위험하다.
  - **없나** 를 볼 때 — 주석 때문에 괜히 빨개진다. 시끄럽지만 안전하다.

  실제로 난 사고의 형태:

  ```js
  // §6 — 바인딩은 인라인 ``onclick="go(2)"`` 하나뿐이라 그대로 옮긴다
  <button ...>            // ← onclick 을 실수로 빼먹었다
  ```
  ```python
  assert 'onclick="go(2)"' in html      # 주석이 통과시킨다 (초록)
  ```

- 개별 테스트마다 따로 처리하면 또 놓친다. 주석 제거 정규식을 테스트 안에서
  다시 만들면 `TestSourceCheckHelperIsTheOnlyOne` 이 빨개진다.

## 검사 습관

- **되돌리기로 확인한다.** 새 테스트를 쓰면 고치기 전 상태로 되돌려 실제로
  빨개지는지 본다. 015 §A 에서 동시성 테스트 두 개가 망가진 코드에서도
  통과했다 — 초록은 증거가 아니다.
- **판정 숫자는 `tools/freeze_check.py --diff` 로 증명한다.** "건드릴 이유가
  없다" 와 "안 건드렸다" 는 다른 말이다.
- 헤드리스 브라우저의 `--window-size` 는 레이아웃 뷰포트를 안 바꾼다(496 고정,
  스크린샷만 잘라낸다). 진짜 폭이 필요하면 `<iframe>` 으로 만든다 (028).
